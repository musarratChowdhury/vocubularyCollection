{
  "name": "nodejslearning",
  "version": "1.0.0",
  "description": "trying to implement server side programming on a basic app",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "nodemon server.js --ignore info.json"
  },
  "author": "musarratChowdhury😎",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^4.1.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.12"
  }
}
